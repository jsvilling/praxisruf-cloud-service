version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build started on `date`
      - gradle build
artifacts:
  files:
    - build/libs/praxis-intercom-cloud-service-0.0.1-SNAPSHOT.jar
  discard-paths: yes
